# /Pending_Approval/local/

Draft outputs from the **Local agent** (Claude Code) awaiting human review.

Same HITL gate as the top-level /Pending_Approval/ — these are tasks
originated and drafted locally (WhatsApp replies, payments, file deletes).
